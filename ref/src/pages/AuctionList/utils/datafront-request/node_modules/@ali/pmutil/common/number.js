/**
 * 数字相关
 */


/**
 * 截断一个数字
 * @param  {Number} number 要截断的数字
 * @param  {Number} maxLen 最多有多少个数字
 * @return {Number}        截断后的数字
 */
exports.trunc = function(number, maxLen) {
  var numberLen = Math.floor(number).toString().length;
  return parseFloat(number.toFixed(maxLen - numberLen));
};