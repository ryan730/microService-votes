/** @jsx createElement */
import _ from '../index';
import { createElement, render } from 'rax';
import View from 'rax-view';
import Text from 'rax-text';
import Button from 'rax-button';

const home =
  '//market.m.taobao.com/apps/pm/paimai/index.html?wh_weex=true&wx_navbar_transparent=true&data_prefetch=true';

const styles = {
  btn: {
    margin: 25,
    borderWidth: 1,
    height: 100,
    borderStyle: 'solid',
    borderColor: '#999999',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundImage: 'linear-gradient(to right,#D72850,#AE1326)',
    borderRadius: 10
  },
  title: {
    fontSize: 28,
    color: '#ffffff'
  }
};

render(
  <View>
    <Button
      style={styles.btn}
      onPress={() => {
        _.setPageSPM('a2129', '1234567890');
      }}
    >
      <Text style={styles.title}>setPageSPM('a2129', '1234567890')</Text>
    </Button>
    <Button
      style={styles.btn}
      onPress={() => {
        _.openNewPage(home, 'test', 'openNewPage');
      }}
    >
      <Text style={styles.title}>openNewPage(home, 'test', 'openNewPage')</Text>
    </Button>
    <Button
      style={styles.btn}
      onPress={() => {
        _.openTel('10086');
      }}
    >
      <Text style={styles.title}>openTel('10086')</Text>
    </Button>
    <Button
      style={styles.btn}
      onPress={() => {
        _.replace(home, 'test', 'replace');
      }}
    >
      <Text style={styles.title}>replace(home, 'test', 'replace')</Text>
    </Button>
  </View>
);
