import { setPageSPM } from '@ali/universal-spm';
import { openNewPage, open, replace, openTel } from './navigator';
import { getParam, getSpmUrl, updateQueryString } from './url';

/**
 * 请使用 `@ali/universal-pm-common-url-router` 组件
 * @deprecated
 */
function push() {
  console.warn('`push` 方法已迁移至 `@ali/universal-pm-common-url-router` 组件');
}

/**
 * 请使用 `@ali/universal-pm-common-url-router` 组件
 * @deprecated
 */
function getFullPath() {
  console.warn('`getFullPath` 方法已迁移至 `@ali/universal-pm-common-url-router` 组件');
}

export default {
  /**
   * @deprecated
   */
  push,
  replace,
  openTel,
  getSpmUrl,
  /**
   * @deprecated
   */
  getFullPath,
  openNewPage,
  open,
  getParam,
  updateQueryString,
  setPageSPM
};
